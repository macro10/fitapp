# fly.toml app configuration file generated for backend-spring-darkness-9130 on 2025-06-19T19:04:11-04:00
#
# See https://fly.io/docs/reference/configuration/ for information about how to use this file.
#

app = 'fitapp'
primary_region = 'iad'
console_command = '/code/manage.py shell'

[build]

[deploy]
  release_command = 'python manage.py migrate --noinput'

[env]
  PORT = '8000'
  DJANGO_SETTINGS_MODULE = 'backend.settings.production'
  # set your domains here (production.py derives CORS from this)
  ALLOWED_HOSTS = 'fitapp.fly.dev,www.fitapp.fly.dev'
  DB_CONN_MAX_AGE = '60'

[http_service]
  internal_port = 8000
  force_https = true
  auto_stop_machines = false
  auto_start_machines = true
  min_machines_running = 1
  processes = ['app']

  [http_service.concurrency]
    type = "requests"
    soft_limit = 20
    hard_limit = 40

[[vm]]
  memory = '1024mb'
  cpu_kind = 'shared'
  cpus = 1